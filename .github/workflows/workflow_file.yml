name: Workflow Name
on: [push, pull_request]

jobs:
  job_name:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Translate Issues
        uses: usthe/issues-translate-action@v2.7
        with:
          token: ${{ secrets.CREDENTIALS_SECRET }}
