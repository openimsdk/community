#!/bin/bash

# Build the Docker image for service1
docker build -t service1 ./service1

# Build the Docker image for service2
docker build -t service2 ./service2

# Build the Docker image for service3
docker build -t service3 ./service3
